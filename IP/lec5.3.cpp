#include<iostream>
using namespace std;
main()
{
    int arr[10],i,n;
    cout<<"enter numbers of element of array :"<<endl;
    cin>>n;
    for(i=0;i<n;i++)
    cin>>arr[i];
    for(i=0;i<10;i++)
    {
     if(arr[i]==arr[i+=1]);

     }
    }
    cout<<"count duplicate numbers :"<<duplicates;

    return 0;
}
